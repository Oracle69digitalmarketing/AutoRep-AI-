service: autorep-backend
provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
functions:
  main:
    handler: src/lambda.handler
    events:
      - http:
          method: any
          path: /{proxy+}